<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <div>{{ text }}</div>

        {% for i in texts %}
            <div>{{ i }}</div>
        {% endfor %}
    </body>
</html>
